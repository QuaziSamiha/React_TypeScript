- The useMemo hook is used to memoize a computed value. This can be useful to optimize performance by preventing unnecessary recalculations when a component re-renders.

- useMemo is a hook that takes two arguments:
  1.  A function that returns a computed value.
  2.  A dependency array that lists variables that, when changed, will re-trigger the computation.
